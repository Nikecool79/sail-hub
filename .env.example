# Google Sheets — each club gets its own Sheet with a Settings tab
VITE_GOOGLE_SHEET_ID=your_google_sheet_id_here
VITE_GOOGLE_API_KEY=your_api_key_here

# All other club config (name, address, coordinates, social links, feature flags)
# is read from the "Settings" tab in the Google Sheet.
# See src/config/clubConfig.ts for the full list of settings keys and defaults.
